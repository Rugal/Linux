# .bashrc

# User specific aliases and functions

#alias rm='rm -i'
#alias cp='cp -i'
#alias mv='mv -i'

# Source global definitions

PS1="[\[\e[0;36m\]\u@\h\[\e[m\] \[\e[0;35m\]\W\[\e[m\]]> "

alias c="clear"
alias ll="ls -lhtcFbsSp --color=auto"
alias l="ls -cFthp --color=auto"
alias cl="clear;ll;pwd"
alias lc=cl
alias df="df -h"
alias free="free -m"
alias ccd="cd .."
alias cdd="cd -"
alias dstat="dstat -cdlmnpsy"
alias slurm="slurm -i wlan0"
alias t="translate"
alias ps="ps -ef|grep"
alias v="vim"
alias V="v"
alias matrix="cmatrix -sb"

git config --global alias.lg "log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --"

bind '"\e[A": history-search-backward'
bind '"\e[B": history-search-forward'
#curl -sL get.ascii.io | bash
#wget http://www.geeknote.me/dist/geeknote_latest.deb
#
function install_eclim()
{
    ECLIPSE_HOME="/usr/local/eclipse"
    ECLIM_JAR="$HOME/eclim_2.2.6.jar"
    if [ ! -d $ECLIPSE_HOME  ]
    then
        echo "eclipse does not exists"
        return
    fi
    if [ ! -e $ECLIM_JAR  ]
    then
        echo "eclim_jar does not exists"
        return
    fi
    sudo apt-get install xvfb build-essential
    if [ ! -e $ECLIPSE_HOME/eclimd  ]
    then
        java -Dvim.files=$HOME/.vim -Declipse.home=$ECLIPSE_HOME -jar $ECLIM_JAR install
    fi
    Xvfb :1 -screen 0 1024x768x24 &
    DISPLAY=:1 $ECLIPSE_HOME/eclimd start
}
